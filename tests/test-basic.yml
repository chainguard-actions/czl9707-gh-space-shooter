name: Test basic usage
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up local git remote for push
        run: |
          # Create a bare repo to act as the remote so git push succeeds
          git init --bare /tmp/remote-repo.git
          git remote set-url origin /tmp/remote-repo.git
          # Make sure we're on a branch so push works
          git checkout -b main 2>/dev/null || git checkout main

      - id: action
        uses: .
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          username: octocat
          output-path: gh-space-shooter.gif
          strategy: random
          fps: '40'
          no-amend: 'true'

      - name: Assert output-file is set
        env:
          ACTUAL: ${{ steps.action.outputs.output-file }}
        run: |
          echo "actual: $ACTUAL"
          if [ "$ACTUAL" != "gh-space-shooter.gif" ]; then
            echo "FAIL: expected 'gh-space-shooter.gif', got '$ACTUAL'"
            exit 1
          fi
          echo "PASS"

      - name: Assert output file exists
        run: |
          if [ ! -f "gh-space-shooter.gif" ]; then
            echo "FAIL: expected gh-space-shooter.gif to exist"
            exit 1
          fi
          echo "PASS: gh-space-shooter.gif exists"
